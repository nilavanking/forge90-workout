# Next migration input required

The following approved feature package is still required before Forge90 can be declared feature-complete in GitHub:

- `Forge90-Phase1-Weight-Journey-2026-09-02.zip`

The 2 September IndexedDB/Dexie package is a complete standalone site candidate, but it does not contain the Weight Journey feature package discussed and approved separately.

The 1 September v2 session-controls package has been received and audited. Its equipment-selection and extended active-set behavior must be adapted to the IndexedDB storage layer rather than copied blindly.
