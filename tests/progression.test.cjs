const test=require('node:test');const assert=require('node:assert/strict');const P=require('../forge90-progression-core.js');
test('parses rep ranges and does not invent a no-history target',()=>{assert.deepEqual(P.repRange('3 × 8–10'),{min:8,max:10});assert.equal(P.target([],'8–10','Dumbbells').kind,'new');});
test('keeps load and improves only incomplete sets',()=>{const t=P.target([{status:'completed',weight:20,reps:10},{status:'completed',weight:20,reps:10},{status:'completed',weight:20,reps:9}],'8–10','Dumbbells',[15,20,22.5]);assert.deepEqual(t.sets,[{weight:20,reps:10},{weight:20,reps:10},{weight:20,reps:10}]);});
test('single completed session holds load despite top reps',()=>{const t=P.target([{status:'completed',weight:20,reps:10},{status:'completed',weight:20,reps:10},{status:'completed',weight:20,reps:10}],'8–10','Dumbbells',[15,17.5,20,22.5,25]);assert.equal(t.kind,'improve-reps');assert.deepEqual(t.sets,[{weight:20,reps:10},{weight:20,reps:10},{weight:20,reps:10}]);});
test('warm-up sets do not affect working targets',()=>{const t=P.target([{status:'completed',weight:5,reps:10,type:'warmup'},{status:'completed',weight:20,reps:9,type:'working'}],'8–10','Dumbbells',[15,20,22.5]);assert.deepEqual(t.sets,[{weight:20,reps:10}]);});
test('weight meanings remain equipment specific',()=>{assert.match(P.loadMeaning('Dumbbells'),/each hand/);assert.match(P.loadMeaning('Barbell'),/total loaded/);});
